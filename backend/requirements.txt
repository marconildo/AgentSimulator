fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Agent / orchestration
langgraph>=0.2.50
langchain-core>=0.3.0
langchain-openai>=0.2.0

# MCP (Model Context Protocol)
mcp>=1.2.0
langchain-mcp-adapters>=0.1.0

# Web search tool (optional Tavily integration — 052-web-search-tool)
tavily-python>=0.5.0

# RAG / vector store
langchain-chroma>=0.1.4
chromadb>=0.5.0

# Reranker (054-rag-block-expansion) — local cross-encoder (ONNX, no torch, no key)
flashrank>=0.2.10

# PDF ingestion (user document upload)
pypdf>=4.0.0
tiktoken>=0.7.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.6.0
